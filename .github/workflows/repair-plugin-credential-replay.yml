name: Repair Plugin credential replay secret authority

on:
  push:
    branches:
      - feat/plugin-vault-postgres-driver-v1

permissions:
  contents: write

concurrency:
  group: integration-plugin-credential-replay-repair
  cancel-in-progress: false

jobs:
  repair:
    if: ${{ github.actor != 'github-actions[bot]' }}
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - name: Checkout exact repair head
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          ref: feat/plugin-vault-postgres-driver-v1
          fetch-depth: 0

      - name: Verify bounded predecessor lineage
        shell: bash
        run: |
          set -Eeuo pipefail
          expected_product_parent='d6280b2464be5a1b60625a87f46232f8ef6dd26e'
          test "$(git rev-parse HEAD~9)" = "$expected_product_parent"
          test "$(git diff --name-only "$expected_product_parent"...HEAD)" = '.github/workflows/repair-plugin-credential-replay.yml'

      - name: Set up Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
        with:
          node-version: 22

      - name: Install reproducible dependencies
        shell: bash
        run: |
          set -Eeuo pipefail
          corepack enable
          pnpm install --frozen-lockfile

      - name: Apply reviewed repair and adapt complete secret-store contract
        shell: bash
        run: |
          set -Eeuo pipefail
          git show HEAD~6:.github/workflows/repair-plugin-credential-replay.yml > /tmp/prior-repair.yml
          awk '
            /      - name: Apply replay authority repair and permanent regressions/ { wanted=1; next }
            wanted && /        run: \|/ { script=1; next }
            script && /^      - name:/ { exit }
            script { sub(/^          /, ""); print }
          ' /tmp/prior-repair.yml > /tmp/apply-reviewed-repair.sh
          test -s /tmp/apply-reviewed-repair.sh
          bash /tmp/apply-reviewed-repair.sh

          python - <<'PY'
          from pathlib import Path

          def replace_once(path: str, old: str, new: str) -> None:
              target = Path(path)
              text = target.read_text()
              count = text.count(old)
              if count != 1:
                  raise SystemExit(f'{path}: expected one repair anchor, found {count}')
              target.write_text(text.replace(old, new, 1))

          def replace_count(path: str, old: str, new: str, expected: int) -> None:
              target = Path(path)
              text = target.read_text()
              count = text.count(old)
              if count != expected:
                  raise SystemExit(f'{path}: expected {expected} repair anchors, found {count}')
              target.write_text(text.replace(old, new))

          def insert_verifier(path: str, marker: str, verifier: str) -> None:
              target = Path(path)
              text = target.read_text()
              count = text.count(marker)
              if count != 1:
                  raise SystemExit(f'{path}: expected one verifier anchor, found {count}')
              target.write_text(text.replace(marker, verifier + marker, 1))

          class_paths = [
              'apps/integration-service/src/plugin-credential-revocation-winner-consistency.test.ts',
              'apps/integration-service/src/plugin-credential-malformed-read-evidence.test.ts',
              'apps/integration-service/src/plugin-credential-lifecycle-authority.test.ts',
              'apps/integration-service/src/plugin-credential-durable-record.test.ts',
          ]
          class_marker = '  async deleteSecret(secretReference: string): Promise<void> {'
          class_verifier = "  async verifySecret(\n    _secretReference: string,\n    _input: PutPluginSecretInput,\n  ): Promise<void> {\n    throw new Error('unexpected replay verification in fixture');\n  }\n\n"
          for path in class_paths:
              insert_verifier(path, class_marker, class_verifier)

          object_paths = [
              'apps/integration-service/src/plugin-credential-clock-evidence.test.ts',
              'apps/integration-service/src/plugin-credential-malformed-authority.test.ts',
          ]
          object_marker = '    async deleteSecret(_secretReference: string): Promise<void> {'
          object_verifier = "    async verifySecret(\n      _secretReference: string,\n      _input: PutPluginSecretInput,\n    ): Promise<void> {\n      throw new Error('unexpected replay verification in fixture');\n    },\n"
          for path in object_paths:
              insert_verifier(path, object_marker, object_verifier)

          replace_once(
              'apps/integration-service/src/plugin-vault-postgres-driver-ip-authority.test.ts',
              "        rows: [{ integration_plugin_runtime_ready: 1 }] as readonly Row[],\n",
              "        rows: [],\n",
          )

          postgres_test = 'apps/integration-service/src/plugin-vault-postgres-driver.test.ts'
          replace_once(
              postgres_test,
              '  const query = vi.fn(async (text: string) =>\n',
              '  const query = vi.fn(async (text: string, _values?: readonly unknown[]) =>\n',
          )
          replace_once(
              postgres_test,
              '      return (await query(text, values)) as {\n',
              '      return (await query(text, values)) as unknown as {\n',
          )

          vault_store = 'apps/integration-service/src/plugin-vault-secret-store.ts'
          replace_once(
              vault_store,
              "    body: request.body,\n",
              "    ...(request.body === undefined ? {} : { body: request.body }),\n",
          )
          replace_once(
              vault_store,
              "    if (typeof http !== 'function') {\n      return unavailable();\n    }\n    this.http = http;\n",
              "    this.http = typeof http === 'function' ? http : unavailable();\n",
          )

          replace_once(
              'apps/integration-service/src/plugin-vault-secret-store-verification.test.ts',
              "  return new Response(encoded, {\n    status,\n    headers: encoded === undefined ? undefined : { 'content-type': 'application/json' },\n  });\n",
              "  return new Response(encoded, {\n    status,\n    ...(encoded === undefined\n      ? {}\n      : { headers: { 'content-type': 'application/json' } }),\n  });\n",
          )

          replace_once(
              'apps/integration-service/src/server.ts',
              '  exitCode?: number;\n',
              '  exitCode: string | number | null | undefined;\n',
          )

          replace_once(
              'apps/integration-service/src/plugin-credential-repository-invalid-evidence.test.ts',
              "const BINDING_ID = '44444444-4444-4444-8444-444444444444';\n",
              "const BINDING_ID = '44444444-4444-4a44-8444-444444444444';\n",
          )

          cleanup_test = 'apps/integration-service/src/plugin-vault-secret-store-body-cleanup.test.ts'
          cleanup_old = (
              "      const deletion = store.deleteSecret(REFERENCE);\n"
              "      await vi.advanceTimersByTimeAsync(0);\n"
              "      expect(requestSignal).toBeDefined();\n\n"
              "      await vi.advanceTimersByTimeAsync(5_001);\n"
              "      expect(requestSignal?.aborted).toBe(true);\n"
              "      await expect(deletion).rejects.toBeInstanceOf(PluginVaultSecretStoreError);\n"
          )
          cleanup_new = (
              "      const deletion = store.deleteSecret(REFERENCE);\n"
              "      const rejection = expect(deletion).rejects.toBeInstanceOf(\n"
              "        PluginVaultSecretStoreError,\n"
              "      );\n"
              "      await vi.advanceTimersByTimeAsync(0);\n"
              "      expect(requestSignal).toBeDefined();\n\n"
              "      await vi.advanceTimersByTimeAsync(5_001);\n"
              "      expect(requestSignal?.aborted).toBe(true);\n"
              "      await rejection;\n"
          )
          replace_count(cleanup_test, cleanup_old, cleanup_new, 2)

          logger_test = 'apps/integration-service/src/plugin-vault-postgres-driver-logger-boundary.test.ts'
          replace_once(
              logger_test,
              "      return { rows: [], rowCount: 0 };\n",
              "      return {\n        rows: [{ integration_plugin_runtime_ready: 1 }] as unknown as readonly Row[],\n        rowCount: 1,\n      };\n",
          )
          replace_once(
              logger_test,
              "  it('does not let a failing telemetry sink turn an idle-client failure into an uncaught process error', () => {\n",
              "  it('does not let a failing telemetry sink turn an idle-client failure into an uncaught process error', async () => {\n",
          )
          replace_once(
              logger_test,
              "    createNodePostgresPluginPool(\n      'postgresql://integration:secret@db.example.test:5432/life_os',\n      test.constructor,\n      () => {\n        throw new Error('telemetry sink failure with secret-like detail');\n      },\n    );\n\n    expect(() => test.emitIdleError(credentialBearingNativeError)).not.toThrow();\n",
              "    const pool = await Promise.resolve(\n      createNodePostgresPluginPool(\n        'postgresql://integration:secret@db.example.test:5432/life_os',\n        test.constructor,\n        () => {\n          throw new Error('telemetry sink failure with secret-like detail');\n        },\n      ),\n    );\n\n    expect(() => test.emitIdleError(credentialBearingNativeError)).not.toThrow();\n    await pool.end();\n",
          )
          PY

      - name: Build Integration workspace dependency
        shell: bash
        run: |
          set -Eeuo pipefail
          pnpm --filter @life-os/plugin-sdk build

      - name: Prove focused GREEN and Integration type contract
        shell: bash
        run: |
          set -Eeuo pipefail
          pnpm --filter @life-os/integration-service exec vitest run \
            src/plugin-credential-replay-secret.test.ts \
            src/plugin-vault-secret-store-verification.test.ts \
            src/plugin-credential.test.ts \
            src/plugin-vault-secret-store.test.ts \
            src/plugin-operator-http-authority.integration.test.ts \
            src/plugin-credential-revocation-winner-consistency.test.ts \
            src/plugin-credential-malformed-read-evidence.test.ts \
            src/plugin-credential-lifecycle-authority.test.ts \
            src/plugin-credential-durable-record.test.ts \
            src/plugin-credential-clock-evidence.test.ts \
            src/plugin-credential-malformed-authority.test.ts \
            src/plugin-vault-postgres-driver-ip-authority.test.ts \
            src/plugin-vault-postgres-driver.test.ts \
            src/plugin-credential-repository-invalid-evidence.test.ts \
            src/plugin-vault-secret-store-body-cleanup.test.ts \
            src/plugin-vault-postgres-driver-logger-boundary.test.ts \
            src/server.test.ts
          pnpm --filter @life-os/integration-service typecheck

      - name: Prove complete Integration test suite
        shell: bash
        run: |
          set -Eeuo pipefail
          pnpm --filter @life-os/integration-service test

      - name: Publish causal repair and remove one-shot writer
        shell: bash
        run: |
          set -Eeuo pipefail
          rm .github/workflows/repair-plugin-credential-replay.yml
          expected_paths=$(cat <<'EOF'
          .github/workflows/repair-plugin-credential-replay.yml
          CHANGELOG.md
          apps/integration-service/src/plugin-credential-clock-evidence.test.ts
          apps/integration-service/src/plugin-credential-durable-record.test.ts
          apps/integration-service/src/plugin-credential-lifecycle-authority.test.ts
          apps/integration-service/src/plugin-credential-malformed-authority.test.ts
          apps/integration-service/src/plugin-credential-malformed-read-evidence.test.ts
          apps/integration-service/src/plugin-credential-replay-secret.test.ts
          apps/integration-service/src/plugin-credential-repository-invalid-evidence.test.ts
          apps/integration-service/src/plugin-credential-revocation-winner-consistency.test.ts
          apps/integration-service/src/plugin-credential.test.ts
          apps/integration-service/src/plugin-credential.ts
          apps/integration-service/src/plugin-operator-http-authority.integration.test.ts
          apps/integration-service/src/plugin-vault-postgres-driver-ip-authority.test.ts
          apps/integration-service/src/plugin-vault-postgres-driver-logger-boundary.test.ts
          apps/integration-service/src/plugin-vault-postgres-driver.test.ts
          apps/integration-service/src/plugin-vault-secret-store-body-cleanup.test.ts
          apps/integration-service/src/plugin-vault-secret-store-verification.test.ts
          apps/integration-service/src/plugin-vault-secret-store.ts
          apps/integration-service/src/server.ts
          EOF
          )
          actual_paths=$(git diff --name-only | sort)
          test "$actual_paths" = "$(printf '%s\n' "$expected_paths" | sort)"
          git diff --check
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'fix(integration): verify credential replay secret evidence'
          git push origin HEAD:feat/plugin-vault-postgres-driver-v1
