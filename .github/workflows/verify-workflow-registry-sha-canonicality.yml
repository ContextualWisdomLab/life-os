name: Verify Workflow Registry SHA Canonicality

on:
  push:
    branches:
      - fix/workflow-registry-state-validation-v1

permissions:
  contents: read

concurrency:
  group: life-os-workflow-registry-sha-canonicality
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - name: Checkout exact branch head
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: ${{ github.sha }}
          fetch-depth: 2
          persist-credentials: false
      - name: Guard current workflow-free exact publication
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "$GITHUB_SHA"
          read -r remote_head _ < <(git ls-remote --refs origin "refs/heads/$GITHUB_REF_NAME")
          test "$remote_head" = "$GITHUB_SHA"
          test ! -e .github/workflows/repair-workflow-registry-formatting.yml
      - name: Set up Node 24
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
        with:
          node-version: '24'
      - name: Enable repository pnpm
        run: corepack enable
      - name: Install frozen workspace
        run: pnpm install --frozen-lockfile
      - name: Check focused formatting
        run: >-
          pnpm exec prettier --check
          packages/commercial-readiness/src/workflow-registry.mjs
          packages/commercial-readiness/src/workflow-registry-sha-canonicality.test.mjs
      - name: Prove exact SHA evidence must already be canonical
        run: >-
          node --test
          packages/commercial-readiness/src/workflow-registry-sha-canonicality.test.mjs
      - name: Run complete Commercial Readiness suite
        run: pnpm --filter @life-os/commercial-readiness test
