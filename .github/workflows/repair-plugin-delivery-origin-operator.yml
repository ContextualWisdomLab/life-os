name: Retired Plugin Delivery Origin Operator Repair

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Purpose-complete writer retired; retained source is updated directly."
