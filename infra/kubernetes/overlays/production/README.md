# Production overlay

`kustomization.yaml` is generated and deliberately ignored. Run the deployment renderer with an exact image-digest map; never commit production credentials or mutable image tags here.
