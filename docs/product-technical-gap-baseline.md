# LifeOS product and technical gap baseline

**Baseline date:** 2026-09-08 (KST)  
**Protected-main evidence:** `193a87ef54c3fe6dcda4755bce4d6bc81e3a0297`; exact live protected head must still be rechecked before promotion  
**Status:** code-current evidence baseline carried by active documentation PR #211 until protected integration  
**Primary commercial tracking:** #21, with canonical P0 buyer gaps #55, #129, #130, #209, and #210

This baseline separates protected shipped truth from active or work-in-progress evidence. A branch, mergeable PR, test source, queued check, successful predecessor run, or documentation claim is not shipped capability. Exact unchanged-head evidence must integrate through the live protected-branch policy before promotion.

> **Currentness override — 2026-09-08 plugin credential replay revocation fence.** #245 is now exact `1c65617233f565b624c1c74045bfcc834b74a632`, open Draft/mergeable on #244. Hosted RED `a4f92ceb51d3eb249817745d29f7dc4b57df8667` (run `34158097007`, job `101854004341`) proved that an existing credential replay could return active metadata after the owning installation was revoked during provider-backed secret verification. The causal descendant revalidates the exact installation/workspace/installer and canonical installation instant after provider I/O. Hosted repair run `34158349560`, job `101854767901`, completed focused replay 3/3, plugin-sdk build, Integration typecheck, and the complete Integration suite at the generated source state (56 passed/4 environment-dependent skipped test files; 366 passed/16 skipped tests), then removed its one-shot workflow and normally published `1c656172...`. This is unshipped Integration-scope evidence, not protected-main or repository-wide acceptance; real Vault-backed composed-runtime create/replay/revocation over migrated PostgreSQL remains the next #130 buyer gap.

## Canonical architecture and evidence rules

LifeOS remains a server-backed modular MSA. Planning, Habit, Review, Identity, Calendar, Integration/Plugin, Notification, AI, and other bounded contexts own their persistence, migrations, credentials, repositories, domain events, runtime authority, and recovery behavior. Cross-service SQL, copied domain source, browser-selected tenant authority, mutable sibling dependencies, and deployment-wide credentials used as per-user authority are not acceptable shortcuts.

Authentication and authorization derive workspace/user authority server-side. Durable acceptance is explicit: stale-scope, unsolicited, duplicated, malformed, impossible, conflicting, future-dated, or otherwise non-authoritative evidence cannot silently become domain truth or ordinary not-found. Sensitive access remains purpose-bound and credential values must not enter durable metadata, public errors, logs, model prompts, or retained CI artifacts.

Optional ContextualWisdomLab foundations are consumed only through released/versioned contracts and ACLs. LifeOS domain truth stays in LifeOS. LLM-backed runtime work must consume released contextual-orchestrator authority and the `orchestrator/free` virtual route rather than provider/model/group/paid-fallback policy in LifeOS.

When claims conflict, use this order: protected `main`; live AGENTS/CLAUDE/PRD/TRD/ARCHITECTURE/ADR plus executable code/contracts on that tree; exact-head active PR evidence explicitly marked unshipped; then issues, plans, historical PR bodies, and research notes. `mergeable: true`, a Draft branch, absent workflows on a non-default stacked PR, or external statuses do not constitute repository GREEN.

## P0 buyer gaps

### #209 — authenticated Goal → Project → Task → Habit → Today → Review product journey

Protected main still does not ship the complete defining first-party journey. The dependency-ordered unshipped web stack remains #214 → #237 → #238 → #225 → #226 → #229 → #231 → #232 → #233 → #234. The stack already contains authenticated BFF and durable workspace slices, but each child must preserve current ancestry and reacquire exact-head repository/security/review evidence after prerequisites integrate.

Still required are durable onboarding-to-Goal conversion with explicit local-draft attachment consent; stable authenticated navigation for Today/Goals/Projects/Tasks/Habits/Review/Settings; authoritative Planning/Habit projections in Weekly Review; Settings for locale/timezone/session/notifications/integrations/data rights/account lifecycle without becoming a persistence owner; material-UI Figma/Product Design/Storybook traceability; normal/loading/empty/error/access-denied/offline/conflict/responsive/interaction/accessibility states; KO/EN/JA/ZH/VI/ES/DE/FR screen-key parity with CJK/text-expansion/font-fallback evidence and a DB-versioned translation ledger separate from ontology labels; and release-artifact buyer E2E for new, returning, second-device, conflict, mobile, offline, and keyboard/accessibility journeys.

Applicable buyer-path API/web boundaries require current-head async E2E/k6 evidence against the repository p95 ≤ 20 ms target without sample exclusion or unrealistic warm-cache assumptions. Any miss must be profiled at query/I/O/render/runtime/framework level before stack replacement.

### #55 — tenant export and deletion orchestration

Protected main has Identity-owned orchestration and the versioned `life-os.data-rights-contributor.v1` boundary. Active #198 exact `8a9e417b15f89f972e242e07197a3a4a818ff82a` and #199 exact `f2669410df648acd876904033d691353a790b9a2` add Notification- and AI-owned contributors as non-force descendants of current protected main, but both remain unshipped until their unchanged exact heads satisfy all applicable repository/security/review gates.

Planning transaction/runtime and export/erasure-evidence safety has an unshipped repair lane in #219 exact `47b8363741c4c2476d34fd2a92526427e0939f96`. Its preserved lineage serializes concurrent SQL on one node-postgres transaction connection, revokes the callback SQL capability before pool release, snapshots parameter arrays when a query receives queue authority, drains every already-admitted query before transaction control, makes any already-admitted query failure transaction-fatal before `COMMIT`, and preserves one-shot node-postgres shutdown authority. Transaction-acceptance RED `d1361e22e412b01d7ece51e581d3934a017b1297` → repair `1bde59441c767c59614abeb8a71330a010df839d` remains intact. Temporal-evidence RED `f762bb16697c10d9eb5aade153c6f15e1c94a19e` → repair `4969afcf324703990e8836a4968578a2eb5d1ec0` requires persisted string timestamps to survive an unchanged finite ISO-UTC round trip and validates `YYYY-MM-DD` through a UTC calendar round trip. Integer-evidence RED `90b2aaf320dde57fd841ec6d78f91c4e40e9c863` → repair `a47fd221b07cd1d1eeee9944e7c3ba84c3ef6340` requires actual nonnegative JavaScript safe-integer count evidence instead of application-side string coercion. Digest-evidence RED `2136255266ad004eeccd3a10952af8be23199697` → repair `cc96fe925026254d64f6ea8cad422a59e30c431e` requires original lowercase 64-hex digest evidence rather than application-side recanonicalization. Today-payload RED `8990fda1e7b10547a506c2a49d701e857dad4fcf`, refined by `22a26d2f61b10ad83362bcf1327b4662f25abb7f`, drives the real export path with a persisted aggregate whose payload date contradicts its `local_date` and with payload values that become valid only after Today-domain canonicalization. Causal repair `952ace9ba38f370a014e19282de704f13e13f705` validates aggregate and idempotency payloads through `canonicalTodayDraft` and rejects any payload whose canonical JSON changes, so malformed durable Today state cannot be rewritten into export/digest authority. Result-kind RED `ea74805353c4011c1002507efc0f85850e82e943` drives `replayed`, `CREATED`, padded ` created `, and `other` through the real export transaction while retaining canonical `created`/`updated` positive controls; predecessor generic `requireString()` accepts those malformed non-empty discriminators despite the Today migration and repository contract allowing only `created` or `updated`. Causal repair `98b031ff0d32b881b77174ebdf67b358b93cbdde` adds the exact durable discriminator boundary. UUID-evidence RED `4e8fe8e992739340e8e8d6ad1bf6dc0a687c466e` drives uppercase persisted PostgreSQL UUID text through the real export transaction; predecessor `requireUuidV4()` accepts it and silently lowercases it into valid-looking export authority. Causal repair `9f2baf96e5e963e3c8794a7abf985eb347f03557` separates request normalization from durable evidence acceptance: exported and erasure-replay UUIDs must already equal their canonical lowercase UUIDv4 representation, while untrusted request IDs retain their existing normalization boundary. CHANGELOG descendant `47b8363741c4c2476d34fd2a92526427e0939f96` records the durable UUID contract. Exact-head #219 CI `34050381829`, Commercial Readiness `34050381751`, Security Scan `34050381767`, CodeQL PR `34050381766`, AppGuardrail `34050381785`, and SAST Semgrep `34050381762` are queued; focused source/fixture evidence is not repository GREEN and predecessor workflow evidence does not transfer.

Completion requires an explicit inventory of every persistence owner, bounded deterministic export, preflight and replay-safe owner-verified erasure, retention/legal hold and backup-expiry outcomes, encrypted authenticated expiring artifact delivery, durable partial-failure recovery, and final success only after the exact required participant set reconciles.

#198 exact `8a9e417b15f89f972e242e07197a3a4a818ff82a` now has terminal GREEN Commercial Readiness run `33925458538`. CI `33925458505`, Security Scan `33925458508`, SAST Semgrep `33925458570`, and CodeQL PR `33925458504` remain non-terminal. AppGuardrail run `33925458503` is terminal RED after a real hosted runner execution in which checkout, scanner execution, detector-contract verification, SARIF upload, and retained-evidence upload succeeded and only enforcement failed. Retained artifact `9960678559` has SHA-256 `3e6fe87e28ce13c41e6f1430d5b1a03071a5f7e810935c3607f15d70e67ef6fd`. Artifact-backed RCA assigns the failure to scanner-owner issue `ContextualWisdomLab/appguardrail#1106`: shell/psql variable indirection was classified as a literal password, explicit `*.test.ts`/`*.test.mjs` code under source-shaped paths was promoted to deploy-blocking application context, credential-free `postgresql://runtime.invalid/...` test sentinels were treated as hardcoded credentials, and an ordinary authority-validation comment was classified as skip-auth intent. LifeOS must not weaken the gate or cosmetically rewrite valid security code solely to satisfy those false positives; consume only an immutable reviewed owner repair, bump the exact scanner pin, and rerun the unchanged contributor lineage.

### #129 — per-user Calendar credential lifecycle

Protected main provides Calendar-owned connection persistence, scoped signed user authority, encrypted self-hostable secret storage, credential materialization, disconnect/read foundations, and provider adapters. Hosted production must not use deployment-wide provider credentials as request authority.

Active #216 exact `217370c8effcd96b760e7b32f4cf4f117385133d` is a non-force descendant of current protected main and removes deployment-wide Google/CalDAV credential authority from hosted composition; its exact-head CI, Security Scan, SAST Semgrep, CodeQL PR, Commercial Readiness, and AppGuardrail runs are queued. Stacked #228 exact `0da52b857f37515c002d4c8e73442f53f89ced10` is ahead-only of that current parent and owns the five-minute Google OAuth state + PKCE authority. Fresh RED `8d7aa2e4f75eff407af1ab380b77679b5d162775` → repair `2b7250aa18e6c7e8aa919b54c9f65373381bc09e` closes a persistence-evidence boundary where a Proxy-backed consumed state row could throw credential-bearing native detail before verifier materialization; the repaired application revalidates all consumed-row fields inside a fixed dependency-failure boundary. #228 remains a non-default stacked Draft with no exact-head PR workflow runs, submitted reviews, or inline threads, so none of that is GREEN evidence.

Remaining work is concrete PostgreSQL OAuth-state persistence/migration, bounded token exchange/refresh with single-flight fencing, successful post-exchange verifier deletion and failure reconciliation, authenticated hosted callback/provider composition from exact active connection evidence, revocation/KMS cleanup recovery, provider calendar discovery/selection, restart/key-rotation evidence, and end-to-end user-scoped synchronization.

### #130 — Plugin installation, secrets, and outbound delivery

Protected main owns versioned plugin SDK/manifest preparation, host-authorized installation/grant/revocation, PostgreSQL installation evidence, credential binding, and opaque secret-reference boundaries. A manifest is intent, not capability or destination authority. LifeOS keeps Integration/Plugin domain truth, persistence, migrations, credentials, runtime authority, and recovery inside the bounded context; no Calendar secret-store source copy, cross-service SQL, generic `DATABASE_URL`, process-wide `PG*` authority, or mutable sibling dependency is accepted.

The current unshipped dependency stack remains #205 → #235 → #241 → #242 → #243 → #244 → #245. #244 is exact `f0b91869f306b1b7d0d5759307acf347e413aa06`, Draft and mergeable on #243 exact `d03b886f67552e6bb2b53352087d2041dedba07c`; it owns the hosted Integration runtime seam, one service-owned SQL pool, exact `INTEGRATION_DATABASE_URL`, pre-listener authenticated Vault-backed operator composition, bounded lifecycle/cleanup, and concrete `PluginOperatorApplication` acceptance. ADR 0005 remains Proposed because the concrete driver and real PostgreSQL/Vault acceptance are not protected shipped truth.

Concrete successor #245 is currently exact `0a6fe7b25664bc893a4ebeb9647d830b59202fc2`, open Draft and mergeable. Its accepted source tree remains the Integration-owned ten-path runtime delta: an invalid whole-lock mutation `c78de4f61a8fdfbeaa11d9bba09d64391edce0f6` was neutralized by non-force descendant `0a6fe7b...`, restoring canonical lock blob `ac72584e4cc0a7307aee08b15a71f468eb7cdba2` without treating that failed attempt as product progress. The preserved runtime tree provides a concrete node-postgres/default-entrypoint adapter with explicit self-contained PostgreSQL URI authority; URI query/fragment denial; verified TLS configured separately from the URI; finite Pool connection/idle/lifetime plus server `statement_timeout` and client `query_timeout`; bounded credential-free process startup failure; bounded idle-error SQLSTATE evidence and telemetry failure; constructor/listener acquisition cleanup; and a fixed readiness `SELECT 1 AS integration_plugin_runtime_ready` before runtime authority crosses the hosted boundary.

The latest accepted runtime authority also treats node-postgres objects/results as untrusted capability evidence. Readiness envelope/rows/row-marker access is fail-closed so throwing or revoked Proxy evidence cannot leak native/credential detail or skip acquired-Pool cleanup. `pool.query` is captured exactly once and the same receiver-bound callable is used for readiness and all accepted runtime SQL. RED `95c23186b09114e4ae01b6a6eaf1c8e4aec126ba` → repair `0c6065378a57c99574694e203463b5111847238c`; the current tree-equivalent branch head `0a6fe7b25664bc893a4ebeb9647d830b59202fc2` preserves the accepted shutdown boundary by capturing `pool.end` once during acquisition and reusing that exact authority for acquisition rollback, readiness cleanup, and runtime shutdown. Throwing/non-callable shutdown accessors fail through `PluginNodePostgresConfigurationError` without reflecting native detail.

This remains deterministic source/fixture evidence only. #245 has no exact-head PR workflow runs or independent approval, and no real PostgreSQL/Vault environment was contacted for the source repairs. Package/repository/100% coverage GREEN, migration acceptance, actual timeout timing, TLS certificate/hostname/handshake acceptance, load/p95 evidence, restart/recovery evidence, and protected-lineage runtime acceptance are not claimed.

A real reproducibility RED remains intentionally open: `a0b5653426f0d55e50f6baff6ad66543a57d932f`. `apps/integration-service/package.json` declares `pg ^8.22.0` and `@types/pg ^8.20.0`, while the root `pnpm-lock.yaml` `apps/integration-service` importer still lacks both direct entries. The shared lock graph already resolves `pg 8.22.0` and `@types/pg 8.20.3`. Earlier broad regeneration `f3bc29112e691e1c30008c4e4132b486f20567ff` changed unrelated lockfile data and was reversed by `b6c1befd8fea6b9943d95461b9f19b1d9f4f5061`; the later whole-file attempt `c78de4f61a8fdfbeaa11d9bba09d64391edce0f6` was likewise rejected and exactly restored by `0a6fe7b...`. Neither is GREEN evidence. The next causal change remains an atomic importer-only frozen-lock repair for `pg ^8.22.0 → 8.22.0` and `@types/pg ^8.20.0 → 8.20.3`, without weakening frozen-lock verification or regenerating unrelated graph state.

After that importer repair, protected-lineage acceptance must exercise real PostgreSQL + Vault migrations, readiness/startup/shutdown, connection limits, statement/query timeout behavior, idle-error and hostile-result cleanup, verified TLS certificate/hostname/handshake behavior, and service-owned credential resolution. Only then should #130 advance to independently reviewed connect-time SSRF/DNS-rebinding-safe outbound HTTPS or an immutable reviewed egress contract, redirect/proxy denial, finite byte/deadline limits, signing/idempotency, durable attempt/outcome/retry/dead-letter/recovery evidence, revocation fencing, operator recovery, and buyer-visible delivery state.

#### Currentness override — 2026-09-08 credential replay publication

The historical #130 evidence above remains audit history. The authoritative active-stack override is now #205 → #235 → #241 → #242 → #243 → #244 → #245. Parent #244 remains exact `f0b91869f306b1b7d0d5759307acf347e413aa06`, open Draft/mergeable on #243. Concrete successor #245 is exact `497bad35c0d7d53febd980aed6393c2433c25318`, open Draft/mergeable and ahead-only of #244. The importer-only frozen-lock repair and real PostgreSQL 17.10 verified-TLS acceptance are published; DNS hostnames remain certificate-identity-bound while IPv4/IPv6 literal database authorities fail closed before acquisition.

#245 also repairs the Plugin credential replay durable-acceptance gap. An existing binding can no longer return active metadata merely because its durable identity matches: replay must prove through the `PluginSecretStore` provider boundary that the opaque secret reference still contains canonical immutable binding evidence and the exact incoming secret bytes. `PluginVaultSecretStore` performs bounded KV v2 GET validation and constant-time secret comparison. Missing, malformed, or conflicting provider evidence fails closed. Secret bytes and secret digests remain outside LifeOS durable metadata.

Hosted repair evidence is explicit rather than promoted beyond scope. Run `34156755735`, job `101850074109`, proved plugin-sdk build, Integration typecheck and the complete Integration suite GREEN with 56 passed/4 skipped test files and 365 passed/16 skipped tests before a publication-only harness defect. Final run `34156840579`, job `101850321540`, repaired that delta gate and completed frozen install, reviewed repair application, build/typecheck/full Integration suite, bounded-delta validation, one-shot self-removal and normal publication GREEN. Bot child `497bad35...` is the resulting product head and the temporary writer is absent from its tree.

That evidence is not real Vault-server acceptance and does not make the stacked branch repository-wide GREEN. Exact published #245 currently has no repository workflow runs of its own and no independent approval/thread authority. The next #130 buyer gap is real Vault-backed create/exact replay/revocation over migrated PostgreSQL, restart/failure/cleanup and recovery evidence, followed by connect-time SSRF/DNS-rebinding-safe outbound HTTPS, redirect/proxy policy, signing/idempotency, durable attempt/outcome/retry/dead-letter recovery, revocation fencing, operator recovery, and buyer-visible delivery state.

### #210 — reproducible release candidate

Source and CI are not a release. Active release work #217 with stacked #236 remains unshipped. A release-ready exact protected head must produce and verify version/CHANGELOG/tag/package identity, digest-pinned OCI/migration artifacts, SPDX 3.0.1 SBOM, SLSA provenance with exact attained level, checksums/signatures, vulnerability evidence, reproducible install/upgrade/rollback, encrypted backup/restore with measured recovery evidence, and exact buyer journeys executed from immutable release artifacts. Stable/GA is not credible while another canonical P0 buyer gap remains open.

## Commercial-readiness and control-plane boundary

PR #230 hardens read-only Actions workflow-registry evidence; issue #202 owns authorized orphan workflow-state mutation. LifeOS-owned lanes remain subject to live organization rules and central reusable workflows. Queued or absent evidence is not success, and central scanner/runner defects are repaired at their canonical owner rather than through leaf workflow weakening.

LifeOS control-plane foundation #247 is exact `c8e124d2a514762a48e7764e20dea30bdd484a56`, an ahead-only non-force descendant of protected `main@193a87ef54c3fe6dcda4755bce4d6bc81e3a0297`. It keeps Draft/Ready queue cancellation, recursive Markdown-only AppGuardrail admission, bounded GET-only 500/502/503/504 retry, mutation exactly-once semantics, current-policy exact-head approval checks, fail-closed mergeability-state handling, Actions evidence binding to explicit PR association, contradictory-Draft fail-closed behavior, exact SHA provenance for commit statuses, review-thread pagination/state validation, stable workflow/status/review pagination, explicit merge-response provenance, a snapshot schema that retains exact review commit binding, independent-review identity rejection, and final mutation/base-freshness fences that bind both protected-main authority and the exact GitHub compare pair.

Findings #31–#52 are code-current on the active #247 lineage. Finding #31 preserves review `commit_id` through snapshot validation without fabricating valid approval authority. Finding #32 makes multi-page review collection revalidate its first-page authority so concurrent dismissal or mutation fails closed. Finding #33 adds only `pull-requests: read` to the scheduled/manual merge drain because `commandDrain()` reacquires PR metadata, reviews, and review threads immediately before mutation. Finding #34 keeps readiness-issue publication as reporting rather than merge authority. Finding #35 pins at most one already-evaluated eligible PR per mutating drain so a successful first merge cannot make the same invocation lose its durable receipt on a second stale-authority attempt.

Finding #36 rejects non-string decisive-review `submitted_at` evidence before JavaScript parsing. Finding #37 preserves same-reviewer chronology across stale approvals and equal GitHub timestamps. Finding #38 requires decisive-review timestamps to use GitHub's canonical UTC second-precision shape and survive an unchanged UTC calendar round-trip. Finding #39 applies the same canonical timestamp rule to merge-authoritative commit-status ordering. Finding #40 closes collector-side review identity coercion: RED `101d7363abc7a9965211e75f2af361c63eb4c459` supplies array-valued reviewer login, review state, and approval `commit_id`; predecessor `String(...)` normalization turns those arrays into a valid reviewer, `APPROVED`, and exact head. Causal repair `db39ce38ffc67a7d2452946f8ee64601aaed1980` preserves the scalar-type boundary so malformed decisive identity becomes invalid review evidence and cannot grant exact-head approval.

Finding #41 extends the same fail-closed scalar boundary to the remaining merge-authoritative collector surface. RED `3a15a2ccafc4d8d60db8ee77ec8d2abdfbaa7218` covers commit-status context/state/SHA coercion, RED `f3a76e4d595ef8883aa09487f236d665a60bafb5` covers workflow name/status/head-SHA coercion, and RED `a30b309a7fbaf9dc274be36dc537ad56210e5e97` covers pull-request state/mergeability/base/head-repository/head-SHA coercion. Causal repair `bfece565d6b899c5468c9620317363943d4922be` retains scalar types and fails malformed decisive evidence closed.

Finding #42 closes the merge-mutation SHA provenance fence. RED `4ee8256ec79afd34abae7c6ebf90afbac1bbb497` demonstrates that array-valued live protected-branch SHA evidence could be coerced by predecessor `String(...)` into the exact workflow `GITHUB_SHA`. Causal repair `e45ae290fcc063cd34c47ffd0e9e38e7f718d20a` requires an original string before SHA/equality checks; CHANGELOG descendant `d47e3ebf555c0641ad50e9b438bdadfebf7b41ad` records the contract. Focused predecessor RED and repaired control-flow GREEN are not repository-wide GREEN.

Finding #43 converts the first hosted merge-compatibility RED after those hardening changes into a bounded test-contract repair. On predecessor `d47e3ebf555c0641ad50e9b438bdadfebf7b41ad`, CI run `34015761978` successfully resolved live source/base identities, built the fresh integration tree, completed frozen-lock installation, and passed repository typecheck before `pnpm test` exposed two stale commercial-readiness assertions. `github-client-status-provenance.test.mjs` still expected `missing-status:CodeRabbit` although malformed/missing SHA provenance is deliberately retained as invalid exact-head status authority and therefore evaluates as `status-not-successful:CodeRabbit`; `source-verification-workflow.test.mjs` still asserted the obsolete PR-only literal instead of the current Ready/non-Draft merge-compatibility condition. Test-only descendants `1035c2f492531642c4ebca2beac246f5cda9b9ac` and `22e5fc98127ad4c54115f9d2096200b823cb24d8` align those contracts without changing production workflow, security, merge eligibility, or provenance fences. The predecessor failure remains RED evidence and current-head hosted GREEN must be reacquired.

Finding #44 closes base-commit provenance before live-base freshness can be evaluated. RED `4c52a07bd0b1384f1b1fb240d9021d523878ef97` adds a malformed array-valued `base.sha` fixture. Causal repair `5702ae24c1bb79c92ba6f8b2f2d3f1a6e8735abd` requires the original `base.sha` to be a canonical string SHA before constructing the compare request; CHANGELOG descendant `6661a82e6c2e634e4f11a8cb7e0df768bb785f38` records the production contract.

Finding #45 closes the remaining live protection-state gap in the final merge fence. RED `838f1cd12f0b996d74dcb5eff2d769d9e7f275f4` proves a same-SHA branch response remained mutation-authoritative when `protected` was false or malformed. Causal repair `cf4ea4a226ad8431aecb97e2fa25686d9dff99d0` additionally requires exact boolean `protected: true`; CHANGELOG descendant `366bb083827461523644beeace53ea01b84ae0cc` records the production contract.

Finding #46 binds base-freshness authority to the exact compare response rather than trusting a standalone `behind_by`. RED `eb801ca3e6e55a724ce469153bf2f7ff707a4005` supplies `behind_by: 0` with a mismatched `base_commit.sha`; predecessor collection grants zero-behind authority, while the required contract is fail-closed `behind_by: -1` and `base-out-of-date`. Causal repair `e69e07532c098c5db701457070af2081655343e3` requires GitHub's canonical compare URL and exact scalar `base_commit.sha`, and when `behind_by === 0` also requires `merge_base_commit.sha` to equal the requested base. Test descendant `cd0f9aacd28ddee2f8e9aa884ce1642ed8b68642` covers mismatched base, wrong-head URL, wrong merge base, and the valid exact comparison. CHANGELOG descendant `23a8c007470b1b9373b4c5241dbed37b4479ab8e` records the production contract.

Finding #47 closes independent-review identity provenance in the local merge collector. RED `cc8f50fc076668ecf28328b1c0b1e60117054e62` supplies an exact-head `APPROVED` review whose reviewer login is the same as the pull-request author's login and requires fail-closed invalid review evidence plus `missing-approval`, while preserving a distinct reviewer's exact-head approval. Causal repair `5fd0de65340fe72384490d082b1e98c58cc9a892` compares decisive reviewer and pull-request-author logins case-insensitively before review authority reaches the evaluator; self-review becomes bounded invalid evidence rather than synthetic independent approval. CHANGELOG descendant `52ebaa394ffd50b35e6e94f5665e50184857b7d4` records the production contract. GitHub's own protected-branch review contract likewise states that pull-request authors cannot approve their own pull requests; this defense preserves that invariant if upstream JSON is malformed or synthetic. Source-level RED/repair evidence is not repository-wide GREEN.

Finding #48 closes the remaining normalization gap in independent-review identity. RED `c361fb4fa67b8b67c60d5aa574e5c65ea1505aef` supplies reviewer login `author-a ` against author `author-a`: predecessor self-review comparison does not match, then `latestReviewsByActor()` trims the reviewer into `author-a`, allowing synthetic self-approval authority. Refined RED `a0b826fe071a6a3cb0e781958b2987ff5756be4b` also requires decisive approval to fail closed when the pull-request author's login itself is a malformed non-string value, because independence cannot be established. Causal repair `52f7027625d534a6af4d879a8a576b623e3c4beb` requires both decisive reviewer and PR-author logins to be non-empty canonical strings with `trim() === value` before case-insensitive independence comparison; malformed or padded decisive identity becomes bounded invalid evidence and cannot grant approval. CHANGELOG descendant `6308261d9befd44fae05d11e6a6116509eaf7d9f` records the contract. Focused control-flow RED/GREEN is not repository-wide GREEN.

Finding #49 is the hosted integration-contract RED from predecessor exact `6308261d9befd44fae05d11e6a6116509eaf7d9f`. CI `34032503368`, merge-compatibility job `101490150313`, resolved the live source/base identities, built a fresh integration tree, completed `pnpm install --frozen-lockfile`, and passed repository typecheck before `pnpm test` failed only `schema-review-commit-provenance.test.mjs`. The fixture omitted the canonical PR-author identity introduced by findings #47–#48 and returned bare `{ behind_by: 0 }` instead of the exact compare URL/base/merge-base provenance required by finding #46. Test-only repair `6d537f6ab0dc554b3ae8cebc8811c663a7715384` adds a distinct author and canonical compare evidence. Production collector/evaluator/workflow behavior is unchanged.

Finding #50 consumes the separate Semgrep RED at `github-client-docstring.test.mjs:8-11`. The old test helper interpolated a declaration-pattern argument into `new RegExp(...)`, triggering `javascript.lang.security.audit.detect-non-literal-regexp.detect-non-literal-regexp`. The argument was repository-owned test data, but dynamic regex construction was unnecessary. Test-only repair `e14e88c530f9acf1f77e3dc8b0a9197bb1d3172b` removes dynamic regex entirely: it locates an exact declaration marker, extracts only the directly preceding JSDoc with bounded string operations, rejects intervening non-whitespace, and checks the same required contract snippets. There is no Semgrep suppression, production behavior change, or gate weakening. Exact-head SAST Semgrep `34038416847` on that historical head is terminal GREEN.

Finding #51 is the hosted formatter RED that remains open after findings #49–#50. Historical exact `e14e88c530f9acf1f77e3dc8b0a9197bb1d3172b`, CI `34038416809`, validate job `101507896216`, acquired a runner, completed checkout and frozen-lock installation, resolved Prettier 3.9.6, and failed only `Check formatting`. The formatter named exactly `packages/commercial-readiness/src/pr-gate.mjs`, `pr-gate.test.mjs`, and `github-client.mjs`; lint/typecheck/test/build were skipped. The same RED existed on predecessor `6308261d...`, so #49/#50 did not create it. A diagnostic descendant temporarily changed only `format:check` to run Prettier `--write` plus `git diff`, but its CI remained pre-runner queued and produced no authoritative formatter delta. The first restore descendant `29045409ccf50843cec0a3c728f55d73e70314ff` removed the mutating probe but unintentionally narrowed formatter coverage to only those three files.

Finding #52 repairs that diagnostic gate regression without pretending finding #51 is solved. Concurrent descendant `c8e124d2a514762a48e7764e20dea30bdd484a56` restores `package.json` byte-for-byte to protected-main formatter coverage while preserving the non-force lineage. The canonical formatter gate is therefore restored, but the three source files named by the hosted RED still require exact Prettier repair and unchanged-head hosted GREEN.

Current exact #247 head `c8e124d2a514762a48e7764e20dea30bdd484a56` has CI `34050790801`, Commercial Readiness `34050790749`, Security Scan `34050790823`, CodeQL PR `34050790809`, AppGuardrail `34050790878`, and SAST Semgrep `34050790900` queued/pending. Its CI runtime/merge jobs remain pre-runner with no steps, so no predecessor terminal success is reused as current-head evidence. Historical exact `e14e88c...` remains the RCA source for formatter, AppGuardrail, Security, and CodeQL owner failures: AppGuardrail `34038416836` is the known scanner-owner false positive; Security Scan `34038416842` reports shared `qs@6.15.3` CVEs `CVE-2026-82417` and `CVE-2026-82562` owned by #246; CodeQL `34038416821` belongs to central `.github#1925`/`#1927`. Submitted reviews contain no `APPROVED`, and the AppGuardrail owner thread remains unresolved pending immutable `ContextualWisdomLab/appguardrail#1106` repair.

Predecessor exact `6308261d9befd44fae05d11e6a6116509eaf7d9f` produced actionable terminal owner evidence in addition to findings #49–#50. Security Scan `34032503341`, Trivy job `101490150313`, failed on shared `qs@6.15.3`, reporting `CVE-2026-82417` and `CVE-2026-82562`; #246 owns exact-`pnpm@10.15.0` package-manager regeneration to `qs >= 6.16.0`, frozen-lock acceptance, unchanged scanner enforcement, and no unrelated lock churn. Fresh downstream evidence has been handed to #246. AppGuardrail `34032503414` remains a scanner-owner false positive on an explicit `*.test.mjs` regression title and is routed to `ContextualWisdomLab/appguardrail#1106`; LifeOS does not cosmetically rewrite source, suppress the scanner, or weaken the gate.

AppGuardrail owner issue #1106 is grounded in retained #198 exact-head artifact `9960678559` (SHA-256 `3e6fe87e28ce13c41e6f1430d5b1a03071a5f7e810935c3607f15d70e67ef6fd`) from failed run `33925458503` and the #247 AppGuardrail RED `34038416836` / job `101500630470`, which again executed the full scanner/evidence path and failed only enforcement. Owner acceptance requires semantic distinction between variable indirection and literal secrets, explicit test-file context and deploy-blocking production context, credential-free `.invalid` test sentinels and real credential-bearing URLs, and ordinary security-boundary/test wording versus actual skip/disable/bypass/defer intent, while retaining scanner positive controls. The LifeOS workflow continues to pin an immutable scanner commit and keeps detector-contract verification, evidence redaction, SARIF/evidence retention, and deploy enforcement intact until a reviewed immutable owner repair can be consumed.

## Currentness delta — 2026-09-07

The historical detail above remains preserved for audit, while this section is the authoritative active-branch override for facts that advanced after the earlier paragraphs were written.

Planning #219 is now exact `2ab280d0e9b0f84a13611c34a87533dd7ac2270f`, open Draft/mergeable. Receipt-binding RED `9a3b2e5d15a5159a207e02572c4166f7d32cbfa1` → causal repair `d5a10327243b87494c4f401a032cca137af72041` requires replayed erasure receipt digests to bind contributor, workspace, requesting user, request, idempotency key, and erased-record count; CHANGELOG descendant `2ab280d0...` records the contract. Exact-head Commercial Readiness `34057676684`, AppGuardrail `34057676582`, and SAST Semgrep `34057676566` are terminal GREEN; CodeQL PR `34057676547` is terminal RED on the central authenticated-verdict prerequisite, while CI `34057676548` and Security Scan `34057676568` remained queued at the fresh read. This is not repository GREEN.

Finding #51 is now source-repaired on #247 exact `80c48ce04cb593602f27b51db5cddcf917e60428`. Disposable diagnostic #248 exact `e8f03ad19258d36ea7a94ec6f52500dffa2f5476` completed Commercial Readiness `34060027394` and retained artifact `9997490932`, digest `sha256:1cb32a14947d254785bc7b950529d47822186b17fd142d70e52b8996c13767bc`, containing the exact Prettier 3.9.6 delta. The #247 repair commit applies only that three-file formatter delta; destination blobs exactly match the hosted targets: `github-client.mjs` `66c878cacf4a1d8ab52a1c60e6ec7555a504844f`, `pr-gate.mjs` `ababc7266e733cd1b212dbb0ba957c7993f49b65`, and `pr-gate.test.mjs` `51d939cceb592def4e43e1064ac09369994edee4`. Parent→repair is ahead-only one commit with only those three files changed. Finding #52 remains intact because root `package.json` and its full protected-main formatter coverage were not touched. #248 was then closed unmerged as a verified fully-succeeded diagnostic; its useful evidence/source delta is preserved in #247 rather than discarded.

Fresh exact-head #247 runs are CI `34061630331`, Commercial Readiness `34061630283`, Security Scan `34061630282`, CodeQL PR `34061630257`, AppGuardrail `34061630269`, and SAST Semgrep `34061630270`; all were queued at the first current-head read and therefore none is promoted to GREEN here. Submitted reviews contain no `APPROVED`. Exactly one returned current review thread remains unresolved: the known AppGuardrail scanner-owner false positive at `github-client-self-review-provenance.test.mjs:101`, pending immutable `ContextualWisdomLab/appguardrail#1106` repair. Shared Security and CodeQL owner paths remain #246 and `.github#1925/#1927` respectively.

## Documentation, publication, and release truth

AGENTS, CLAUDE, PRD, TRD, ARCHITECTURE, ADRs, DATA_MODEL/ERD/UML/API contracts, SECURITY/THREAT_MODEL, TEST_STRATEGY, OPERABILITY/recovery, RELEASE_AND_MIGRATION, standards/research traceability, CHANGELOG, and this baseline must describe current code maturity rather than future intent as shipped truth. Proposed decisions remain Proposed until their prerequisites execute and integrate.

`docs/index.md` and README links are source prerequisites only. GitHub Pages is not published evidence until repository settings, deployment, and live HTTPS are verified. No active documentation PR, Draft stack, or WIP branch creates a release, certification, product-completeness claim, or production credential.

## Currentness delta — 2026-09-07 07:00 KST

Planning #219 advanced by non-force descendants to exact `e838c9bb4ab6cb2752470331f1e1707462ee4dd1`, open Draft/mergeable. New durable task-status RED `371501ad62817a47af2b0bc23d6aa34cd2255908` exercises the real data-rights export path and proves that `blocked`, case variants, and padded status strings must not become buyer-visible evidence; the Planning schema and repository domain allow only `todo` and `done`. Causal production repair `31c9fefb89a71e2ad1247031449bd27b256e75bf` adds that exact discriminator check with a `+9/-1` one-file delta, and CHANGELOG descendant `e838c9bb...` records the contract. Current-head CI `34063285205`, Commercial Readiness `34063285207`, Security Scan `34063285215`, CodeQL PR `34063285202`, AppGuardrail `34063285186`, and SAST Semgrep `34063285208` are queued/pending; focused RED/repair evidence is not repository GREEN and predecessor workflow evidence does not transfer.

#247 remains exact `80c48ce04cb593602f27b51db5cddcf917e60428`, open/non-Draft/mergeable. Commercial Readiness `34061630283` is terminal GREEN, but AppGuardrail `34061630269` is terminal RED after a real hosted Ubuntu 24.04 run completed checkout, scanner execution, detector-contract verification, SARIF upload, and retained-evidence upload and failed only enforcement. Its sole deploy-blocking HIGH is the known `todo-skip-auth` false positive on explicit test file `packages/commercial-readiness/src/github-client-self-review-provenance.test.mjs:101`; retained artifact `9998003711` has digest `sha256:b28b82a5efd9ab8f94e7038581ee11c03779233b3512ea1bc4f35bb66745538c`. The fresh reproduction was added to canonical scanner-owner issue `ContextualWisdomLab/appguardrail#1106`; LifeOS keeps the gate and review thread intact pending an immutable reviewed owner repair. CI `34061630331`, Security `34061630282`, CodeQL `34061630257`, and SAST `34061630270` remain non-terminal, and no independent `APPROVED` review exists. No normal merge or release authority follows from this state.

## Currentness delta — 2026-09-07 07:40 KST

Planning #219 is now exact `5da3c8cb96299ddc7335b91550c55703d5f7641e`, open Draft/mergeable and ahead-only of protected `main`. The Today migration owns `revision_number` as `bigint NOT NULL CHECK (revision_number >= 1)`, while the export query deliberately returns `revision_number::text` to preserve full 64-bit precision. RED `3366147c31c26ff42605253d552351a2a46d0271` drives `0`, negative, padded/reformatted decimal, non-decimal, and `9223372036854775808` overflow evidence through the real contributor export path and retains canonical `1` plus `9223372036854775807` positive controls. Predecessor generic `requireString()` would accept those non-empty malformed values. Causal repair `a47c49f18edaeec78030d2102fafd704ca6f9305` changes only `planning-data-rights.ts` (`+13/-1`) to require canonical positive decimal text bounded by PostgreSQL's signed 64-bit maximum; formatting-only descendant `c8c3cb1f6c4bd63dc01412644aeea5b3e80a254c` keeps the regression canonical, and CHANGELOG descendant `5da3c8cb...` records the buyer-visible durable-evidence contract. Current exact-head CI `34064574396`, Commercial Readiness `34064574383`, Security Scan `34064574401`, CodeQL PR `34064574394`, AppGuardrail `34064574387`, and SAST Semgrep `34064574366` are queued/pending, so focused RED/repair evidence is not repository GREEN and predecessor workflow evidence does not transfer.

#247 remains exact `80c48ce04cb593602f27b51db5cddcf917e60428`, open/non-Draft/mergeable. Its LifeOS-owned CI/format/test evidence has now settled GREEN: CI `34061630331`, Commercial Readiness `34061630283`, and SAST Semgrep `34061630270`. AppGuardrail `34061630269` remains terminal RED on the known explicit-test-file scanner false positive owned by `ContextualWisdomLab/appguardrail#1106`; Security Scan `34061630282` remains terminal RED on the shared `qs` CVEs owned by #246; CodeQL PR `34061630257` remains terminal RED on central authenticated verdict recovery owned by `.github#1925/#1927`. No independent `APPROVED` exists and the AppGuardrail owner thread remains unresolved, so there is still no normal merge or release authority.

## Currentness delta — 2026-09-07 09:00 KST

#247 advanced by non-force descendants to exact `2529c63c6d95def6f553a95831c4ac2482d13ddb`, open/non-Draft/mergeable. Finding #53 closes final-evaluator SHA aliasing: RED `11d155e1ca160ee654f7589f181914217971647b` → causal repair `82823918ddd0fac84213340397760d3cfb453ea3` removes the case-insensitive flag from the canonical 40-hex merge-authority pattern, with CHANGELOG descendant `6f3014ebc421afc561440c47dd0d23416e6e2699`. Finding #54 closes the durable snapshot counterpart. RED `d206f31e5670b2e2c86791b066fcfa524dba5f98` requires uppercase aliases in snapshot `commit_sha`, pull-request `head_sha`, workflow `head_sha`, and status `sha` to fail while lowercase controls remain valid. Causal repair `e19f6a3a26154c19c947ce6d3c4a976924768a2b` removes only `/i` from `SNAPSHOT_SHA_PATTERN`; compare is ahead-only one commit changing only `packages/commercial-readiness/src/schema.mjs` by `+1/-1`. CHANGELOG descendant `2529c63c6d95def6f553a95831c4ac2482d13ddb` adds one Fixed entry and no production behavior.

Fresh exact-head #247 workflows are CI `34069164322`, Commercial Readiness `34069164381`, Security Scan `34069164429`, CodeQL PR `34069164356`, AppGuardrail `34069164338`, and SAST Semgrep `34069164348`; all are queued at this current-head read and predecessor terminal evidence does not transfer. On predecessor exact `6f3014eb...`, AppGuardrail `34066285177` completed its scanner/detector/SARIF/evidence path and failed only enforcement; retained artifact `9999408601` has digest `sha256:98247e02ae7b52f1ab2bae2d652139c3485330f7cd4f4b15bd58e02bfcf30685`, and the sole deploy-blocking HIGH remains the explicit-test-file `todo-skip-auth` false positive at `github-client-self-review-provenance.test.mjs:101`. Canonical owner `ContextualWisdomLab/appguardrail#1106` now carries this fourth reproduction. Shared `qs` security repair remains #246-owned and authenticated CodeQL terminal-verdict recovery remains `.github#1925/#1927`-owned. No independent approval or normal merge/release authority is claimed.

Planning #219 remains exact `5da3c8cb96299ddc7335b91550c55703d5f7641e`, Draft/mergeable with its revision-number durable-evidence lineage intact. This documentation delta changes only currentness evidence; it does not promote #219 or #247 into shipped truth.

## Currentness delta — 2026-09-07 10:00 KST

#247 advanced by non-force descendants to exact `86e529fa197a494718c9c327eacd12dfdc2f7361`, open/non-Draft/mergeable and `behind_by=0` against protected `main@193a87ef54c3fe6dcda4755bce4d6bc81e3a0297`. Finding #55 closes the remaining collector/mutation SHA-canonicality gap left after #53–#54. RED `53ed0d90e919fe3344764519eccd508748570763` adds focused contracts proving uppercase snapshot commit, pull-request head/base, and expected merge head must be rejected before they can become authority; uppercase compare base/merge-base aliases must not grant zero-behind authority; lowercase canonical evidence remains valid. Causal production repair `deaa5d81b4c712c3d8177c723e238b7b7be74f29` removes only `/i` from `packages/commercial-readiness/src/github-client.mjs` `SHA_PATTERN` (`+1/-1`). CHANGELOG descendant `86e529fa197a494718c9c327eacd12dfdc2f7361` adds one Fixed entry. No gate is weakened and mutation remains exactly-once.

Fresh exact-head #247 workflows are CI `34072232125`, Commercial Readiness `34072232115`, Security Scan `34072232108`, CodeQL PR `34072232174`, AppGuardrail `34072232130`, and SAST Semgrep `34072232111`; all were queued at the first current-head read, so predecessor terminal evidence does not transfer. The last settled AppGuardrail owner reproduction before this head moved completed checkout/scanning/detector verification/SARIF/evidence upload and failed only enforcement on the known explicit-test-file `todo-skip-auth` false positive owned by `ContextualWisdomLab/appguardrail#1106`. Shared `qs` repair remains #246-owned and authenticated CodeQL verdict recovery remains `.github#1925/#1927`-owned. No independent approval, normal merge, or release authority is claimed.

Planning #219 remains a separate Draft/data-rights lane. Active branch evidence stays unshipped until exact-head repository/security/review gates and protected integration complete.

## Currentness delta — 2026-09-07 10:40 KST

#247 advanced by non-force descendants to exact `7be88d686b793f36fdaf373e5f4289f699baf67f`, open/non-Draft/mergeable and `behind_by=0`, `ahead_by=195` against protected `main@193a87ef54c3fe6dcda4755bce4d6bc81e3a0297`, with merge base exactly protected main. Finding #56 closes the remaining final-evaluator reviewer-identity recanonicalization gap: RED `2ba9b7523ad34638b8f508858d7303d63239d851` sends padded decisive actor `reviewer-a ` directly through `evaluatePullRequestForMerge()` and requires `review-evidence-invalid` plus `missing-approval`, while canonical identity remains a passing control. Causal production repair `977b740175e7b62e11fe81dc13bd2a85b356be34` requires decisive reviewer identity to already be a non-empty canonical string and stops trimming durable/replayed evidence into approval authority. CHANGELOG descendants `31f2768dfd61d67a8a97bc2748b626fa0e2429e7` and `7be88d686b793f36fdaf373e5f4289f699baf67f` record the finding and restore the original final-newline shape; combined compare from repair to final changelog head changes only `CHANGELOG.md` by `+1/-0`.

Fresh exact-head #247 workflows are CI `34073758635`, Commercial Readiness `34073758678`, Security Scan `34073758491`, CodeQL PR `34073758652`, AppGuardrail `34073758601`, and SAST Semgrep `34073758679`; all are queued at the current-head read, so predecessor terminal evidence does not transfer. Submitted reviews contain no `APPROVED`; one current unresolved thread remains the known explicit-test-file AppGuardrail false positive at `packages/commercial-readiness/src/github-client-self-review-provenance.test.mjs:101`, owned by `ContextualWisdomLab/appguardrail#1106`. Shared `qs` repair remains #246-owned, authenticated CodeQL terminal-verdict recovery remains `.github#1925/#1927`-owned, and runner/admission health remains `.github#712`-owned. No normal merge or release authority is claimed.

## Currentness delta — 2026-09-07 16:35 KST

#247 advanced by non-force descendants to exact `c18c89f0046bd11c9079be00fc8014d59f1daac9`, open/non-Draft/mergeable. Findings #57–#65 extend the same durable-acceptance principle across workflow ordering, protected-branch and merge-receipt SHA identity, reviewer identity, duplicate required-check evidence, merge receipts, and commit-status identity. Finding #57 rejects non-integer or otherwise coerced workflow `run_attempt` authority. Findings #58–#59 require canonical lowercase SHA evidence at the protected-default-branch mutation fence and in successful GitHub merge receipts. Finding #60 treats case-variant spellings of one reviewer login as ambiguous decisive-review evidence rather than independent reviewer histories. Finding #61 rejects duplicate/contradictory required workflow or status evidence in the final evaluator. Finding #62 retains the canonical GitHub merge-result commit SHA in durable drain receipts. Finding #63 moves merge-response validation into exported `mergeEligiblePullRequests()` itself: fixture alignment `c6316f3436f19c88307e8ec374df7c1ed52917b0`, RED `58579c05d371188af8dec2e663b593324a6c28e5`, causal repair `ad2c9d3944810879f1aaf835cf5db47a6c5c8e6e`.

Finding #64 closes equal-ID contradictory status evidence. RED `f11e014487a9eb27d54f067d69e634ae164c45a6` proves two exact-head `CodeRabbit` records carrying the same positive GitHub status ID can otherwise leave one success authoritative; repair `387874abc6bc5e4a6cade940b5b5822062cc193d` makes duplicate valid IDs fail closed. Finding #65 closes the remaining pre-provenance variant. RED `535eb6e21750bd5d5346f538f152c91334405d8f` pairs an exact-head `CodeRabbit` success with a different-context record reusing the same positive status ID but carrying mismatched SHA provenance; the #64 ordering could discard the mismatched record before ID ambiguity was observed and leave the prior success authoritative. Causal repair `3ad32fab4042eaa3db9a01cb13bb7ff6394425cc` tracks positive status identity before SHA/state/timestamp provenance reduction, so every affected context is tainted even when the duplicate record is stale or malformed. RED→repair is ahead-only one commit and changes only `packages/commercial-readiness/src/github-client.mjs` (`+15/-12`). Focused bounded GREEN covers same-context duplicate, stale cross-context duplicate, and a distinct-ID positive control. CHANGELOG descendant `c18c89f0046bd11c9079be00fc8014d59f1daac9` is source-current through finding #65.

Fresh exact-head #247 workflows are CI `34096170468`, Commercial Readiness `34096170641`, Security Scan `34096170632`, CodeQL PR `34096170493`, AppGuardrail `34096170577`, and SAST Semgrep `34096170585`; all are queued at this read. CI jobs `101660220207` merge_compatibility, `101660220415` compose_runtime, `101660220520` today-concurrency, and `101660220567` browser-acceptance have `steps=[]` and no assigned runner identity. Predecessor terminal evidence does not transfer. Submitted reviews have no independent `APPROVED`; the known AppGuardrail test-title false-positive thread at `packages/commercial-readiness/src/github-client-self-review-provenance.test.mjs:101` remains unresolved and owner-routed to `ContextualWisdomLab/appguardrail#1106`. Shared `qs` repair remains #246-owned, authenticated CodeQL verdict recovery remains `.github#1925/#1927`-owned, runner/admission health remains `.github#712`-owned, and administrator-bypass policy remains `.github#1340`-owned. No normal merge or release authority is claimed.

## Currentness delta — 2026-09-07 17:20 KST

#247 advanced by ordinary non-force descendants to exact `2590fc2b884e8ef5701f60f1fe74a6b5ed8ca946`, open/non-Draft/mergeable. Finding #66 closes duplicate pull-request identity ambiguity in the mutating merge-drain selector. Reality RED `d25644b899277e4ad8ae0e0e236843e91d55455f` proves that two otherwise eligible snapshot records with the same positive PR number are returned twice by the selector, allowing the same candidate to enter mutation processing twice. Causal repair `62d317ee55a0eb138c7ec28565bef9041729d31a` fails closed on repeated positive safe-integer PR numbers before either initial or refreshed candidate selection; RED→repair is ahead-only one commit and changes only `packages/commercial-readiness/src/cli.mjs` (`+15/-2`). A bounded local control-flow reproduction confirms the predecessor returns the duplicate twice and the repaired selector rejects duplicate initial/refreshed evidence while preserving normal pinned-candidate and dry-run behavior. CHANGELOG descendant `2590fc2b884e8ef5701f60f1fe74a6b5ed8ca946` records the durable-receipt boundary. This focused evidence is not repository-wide hosted GREEN.

## Currentness delta — 2026-09-07 23:30 KST

Release-evidence #217 is now exact `db76d54b4a433875f8a2c3321f34cf926bbea2c5`, open Draft/mergeable and ahead-only (`behind_by=0`, `ahead_by=50`) of protected `main@193a87ef54c3fe6dcda4755bce4d6bc81e3a0297`, with merge base exactly protected main. Canonical #210 requires a supported prior-version upgrade, not merely syntactically ordered version metadata. Reality RED `8b426e258f109f506fe06c0c215c1c289f5fc7f4` proves the predecessor accepted a migration range whose `maximum_source_version` equaled the target `0.2.0-rc.1` or was the future/stable `0.2.0`. Causal repair `f4e9ac3f070337aa02293980da845e15d268660d` parses the target release identity once and requires each migration `maximum_source_version` to compare strictly older than that target. The exact predecessor fails the focused Node 22 regression with a missing expected rejection; the repaired source passes 1/1. CHANGELOG descendant `db76d54b...` records the contract. This is structural admission evidence only: no migration was executed and no real-PostgreSQL upgrade/rollback success is inferred.

Dependent signature-verification #236 is exact `e2c18d2412e8f0955eeda08aa1b58d156ad22210`, open Draft/mergeable after non-force two-parent restacks that preserve prior child history and current #217 ancestry. Fresh parent→child compare is `behind_by=0`, merge base exactly current #217, and the same six signature-verification-owned paths remain; the target-binding source/test/CHANGELOG arrive through parent ancestry rather than copied child ownership. #217 exact-head Security Scan `34132932472`, CodeQL PR `34132932271`, CI `34132932461`, AppGuardrail `34132932241`, SAST Semgrep `34132932388`, and Commercial Readiness `34132932366` are queued at the current read, with no submitted reviews or inline review threads. Queued/absent evidence is not GREEN and predecessor workflow evidence does not transfer. No normal merge or release authority is claimed.

## Currentness delta — 2026-09-08 00:50 KST

Release-evidence #217 is now exact `d5a2723765cc3f5e433e99d0d0b4b37a38070f32`, open Draft/mergeable and ahead-only (`behind_by=0`, `ahead_by=59`) of protected `main@193a87ef54c3fe6dcda4755bce4d6bc81e3a0297`, with merge base exactly protected main. The intervening filesystem-authority repair remains in ancestry: RED `8452a9a90f3e824ca88dceb1d5c2e70f85ce6d6f` → causal repair `a690d62be362cb900e82aa73fac994f92738168c` → CHANGELOG `cc5228bb8825a91b07217c60bfe577acbfeda298` rejects a real final evidence directory reached through symlinked ancestry before retained artifacts are opened.

Fresh review against #210 found that the predecessor still treated `SHA256SUMS` as an opaque self-consistent blob. Reality RED `bc81c747b5b048f17d654a5bff90936458f5f4e8` proves a checksum file can have matching indexed bytes/digest and detached-signature subject while listing only an unrelated artifact. Causal repair `685d0f6841b86deead1ae96ed9028b92372eeb71` derives the expected lowercase SHA-256 manifest from the validated retained non-checksum/non-signature artifacts and verifies the exact expected bytes through the same already-open `O_NOFOLLOW` descriptor used for digest/size checking. Existing directory fixtures were aligned in `28b961a2a00ef5367a7b6b2be9c9ab0cc65d6508`; CHANGELOG descendant `97a66304730de54ca4c1cac458fe363cf74178cb` records the buyer-visible checksum binding.

Self-review then found a reproducibility defect in that first repair: `localeCompare('en')` made canonical manifest order depend on ICU collation and differs from code-unit order for valid ASCII artifact names such as `a-.bin` and `a_.bin`. RED `4415e5217cf38d656884e54811d74537a2846983` adds that exact punctuation-order case; causal repair `d5a2723765cc3f5e433e99d0d0b4b37a38070f32` replaces locale collation with explicit ASCII/code-unit ordering while preserving the artifact-name grammar and checksum coverage contract.

Exact-head #217 workflows are Commercial Readiness `34139977974`, CI `34139977931`, CodeQL PR `34139977920`, AppGuardrail `34139977898`, SAST Semgrep `34139977939`, and Security Scan `34139977942`; AppGuardrail is pending and the other five are queued. CI jobs `101799548520`, `101799548704`, `101799548706`, and `101799548733` are exact-head pre-runner jobs with `steps=[]` and no usable runner identity. Submitted reviews and inline review threads are empty. Queued/pending evidence is not GREEN and predecessor evidence does not transfer.

Dependent signature-verification #236 is exact `f3b3ff6322cb37d54281345a7f0674b1f4dbb810`, open Draft/mergeable with base exact current #217 after a non-force two-parent restack. Fresh parent→child compare is `behind_by=0`, merge base exactly current #217, and exactly the same six signature-verification-owned paths remain. The checksum-binding source/tests/CHANGELOG arrive through parent ancestry rather than copied child ownership. Neither #217 nor #236 has normal merge or release authority until exact-head hosted gates, current independent review, prerequisite integration, and the remaining immutable release requirements in #210 are satisfied.
